# pnpm 配置
# 严格对等依赖模式，减少不必要的依赖安装
strict-peer-dependencies=false

# 忽略平台特定的可选依赖
# 这些依赖在其他平台上不会被安装，electron-builder 的警告可以忽略

# 使用 shamefully-hoist 以避免某些模块的兼容性问题
shamefully-hoist=true

# 自动安装对等依赖
auto-install-peers=true
