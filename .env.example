# 数据库配置 (PostgreSQL)
# 在 Vercel/Railway 等平台部署时，平台会自动提供这些变量
DATABASE_URL=postgresql://username:password@host:port/database

# Node环境
NODE_ENV=production

# 应用端口 (通常平台会自动设置)
PORT=5000

# 可选：自定义域名（仅在自托管时需要）
# NEXT_PUBLIC_BASE_URL=https://your-domain.com
